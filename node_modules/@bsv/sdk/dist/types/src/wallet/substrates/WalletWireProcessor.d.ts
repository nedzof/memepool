import { Wallet } from '../Wallet.interfaces.js';
import WalletWire from './WalletWire.js';
/**
 * Processes incoming wallet calls received over a wallet wire, with a given wallet.
 */
export default class WalletWireProcessor implements WalletWire {
    wallet: Wallet;
    constructor(wallet: Wallet);
    private decodeOutpoint;
    private encodeOutpoint;
    transmitToWallet(message: number[]): Promise<number[]>;
    private decodeProtocolID;
    private decodeString;
    private decodeCounterparty;
    private serializeDiscoveryResult;
    private decodeKeyRelatedParams;
}
//# sourceMappingURL=WalletWireProcessor.d.ts.map